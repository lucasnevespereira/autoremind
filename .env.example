# PostgreSQL Database
DATABASE_URL=postgresql://user:password@localhost:5432/autoremind

# App URL (required for better-auth)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Cron Secret (to protect the automatic reminders route)
# Generate with: openssl rand -hex 32
CRON_SECRET=your-random-secret-here

# Note: Twilio settings are configured directly in the app
# through the Settings page
