export enum Env {
  DEVELOPMENT = "development",
  PRODUCTION = "production",
}

export enum LANG {
  EN = "en",
  PT = "pt",
  FR = "fr",
}

export enum PLAN {
  FREE = "free",
  STARTER = "starter",
  PRO = "pro",
}

export enum BILLING_INTERVAL {
  MONTHLY = "monthly",
  ANNUAL = "annual",
}
